export class Pokemon {
  public name: string;
  public sprite: string;
  public id: number;

  constructor(name: string, sprite: string, id: number) {
    this.name = name;
    this.sprite = sprite;
    this, (id = id);
  }
}
